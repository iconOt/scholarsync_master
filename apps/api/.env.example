# Copy to .env and adjust values for local development
# Postgres connection URL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/scholarsync_master

# Optional: PORT the API should run on
PORT=8787
JWT_SECRET=change-me
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/scholarsync_master
